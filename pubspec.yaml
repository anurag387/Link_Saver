name: link_saver
description: "Link Saver — capture instantly, organize effortlessly, find anything later."
publish_to: 'none'
version: 1.1.0+3

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_riverpod: ^2.5.1
  uuid: ^4.4.0
  intl: ^0.19.0
  url_launcher: ^6.3.0
  supabase_flutter: ^2.14.2
  shared_preferences: ^2.2.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
